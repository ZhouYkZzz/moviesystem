server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3307/movie_system?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: xz20220429
    driver-class-name: com.mysql.cj.jdbc.Driver

  data:
    redis:
      host: 127.0.0.1
      port: 6379
      database: 0

mybatis:
  mapper-locations: classpath:/mapper/*.xml
  type-aliases-package: com.moviesystem.entity
  configuration:
    map-underscore-to-camel-case: true

app:
  poster-dir: ${POSTER_DIR:${user.home}/.movie-system/posters}
